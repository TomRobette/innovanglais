{% extends 'base.html.twig' %}

{% block title %}{{ parent()}} - liste niveau{% endblock %}

{% block content %} 
<div class="container-fluid">           
    <div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Liste des niveaux</h1>        
    </div>        
    <div class="row justify-content-center">            
    <div class="col-8 bg-white p-4 m-0 text-dark"> 
    <table class="table table-hover">
    <thead>
        <tr>
        <th scope="col">N°</th>
        <th scope="col">Libelle</th>
        </tr>
    </thead>
    <tbody>
      
        {% for niveau in niveau %}
            <tr class="{{ cycle(['table-info', 'table-light'], loop.index0)}}">

                <td>{{niveau.id}}</td>
                <td>{{niveau.libelle}}</td>

            </tr>
        {% endfor %} 
    </tbody>
                                  
    </div>        
</div></div>    
{% endblock %}