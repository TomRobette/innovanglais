{% extends 'base.html.twig' %}

{% block title %}{{ parent()}} - liste test{% endblock %}

{% block content %} 
<div class="container-fluid">           
    <div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Liste des tests</h1>        
    </div>        
    <div class="row justify-content-center">            
    <div class="col-8 bg-white p-4 m-0 text-dark"> 
    <table class="table table-hover">
    <thead>
        <tr>
        <th scope="col">N°</th>
        <th scope="col">Nombre questions</th>
        <th scope="col">Niveau</th>
        <th scope="col">Liste</th>
        </tr>
    </thead>
    <tbody>
      
        {% for test in test %}
            <tr class="{{ cycle(['table-info', 'table-light'], loop.index0)}}">

                <td>{{test.id}}</td>
                <td>{{test.nbQuestions}}</td>
                <td>{{test.niveau.libelle}}</td>
                <td>{{test.liste.nom}}</td>

            </tr>
        {% endfor %} 
    </tbody>
                                  
    </div>        
</div></div>    
{% endblock %}