{% extends 'base.html.twig' %}

{% block title %}{{ parent()}} - liste langue{% endblock %}

{% block content %} 
<div class="container-fluid">           
    <div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Liste des langues</h1>        
    </div>        
    <div class="row justify-content-center">            
    <div class="col-8 bg-white p-4 m-0 text-dark"> 
    <table class="table table-hover">
    <thead>
        <tr>
        <th scope="col">N°</th>
        <th scope="col">Nom</th>
        </tr>
    </thead>
    <tbody>
      
        {% for langue in langue %}
            <tr class="{{ cycle(['table-info', 'table-light'], loop.index0)}}">

                <td>{{langue.id}}</td>
                <td>{{langue.nom|capitalize}}</td>
                <td><a href="{{path('listeLangue',{'supp':langue.id})}}"><span class="material-icons text-dark" title="Supprimer le test">delete</span></a></td>

            </tr>
        {% endfor %} 
    </tbody>
                                  
    </div>        
</div></div>    
{% endblock %}